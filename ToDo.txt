Kontrastanpassungen ToDo:

// = abgeschlossen

******* Backbone-Tools (diverse):

**** Menu
// -> Inaktive Elemente z.B. HVV, Eignungsflächen, Bohrdaten 
// -> Einstellungen/Zahnrad der Komponenten: % Anzeige und "Layer entfernen"
-> Infobutton -> Metadaten "Publikationen / Erstellung:, Letzte Änderung" ????  TODO

// -> Ausgewählte Layer
// -> Hover Blau auf Schwarz 
Table Style:
    add to config.js: uiStyle: "table",
   // -> icons info and settings
   // -> settings action icons
   // -> Bug: BurgerMenu selected

-----Filter----- TODO -> ist wahrscheinlich nun schon erledigt?

-> Hintergrundfarbe bei ausgewählter Schulart (In MPREFACTOR-386 angegangen, warte auf Merge)
-> Filteroptionen (Bezirk, Stadtteil, etc) (In MPREFACTOR-386 angegangen, warte auf Merge)
-> "Anzahl Schüler" Pfeile
-> Schwerpunktschule Aus

************************* Backbone

**** Backbone tools

// -> parcelSearch / Flurstücksuche
// -> styeWMS / Style WMS
// -> featureLister / Liste
     -> Keine Tastaturbedienung der Tabs möglich -> TODO-Tastatur
     -> keine Tastaturbedierung der Selektion in der Liste -> TODO-Tastatur
// -> filter                     #### selected elements button
      // -> Filteroptionen: Texte als placeholder problematisch < 3:1 
      -> keine oder nur eingeschränkte Tastaturbedienung der verwendeten Snippets -> TODO-Tastatur
// -> extendedFilter / Erweiterter Filter
// -> layerSlider / Zeitreihe
// -> print / Drucken            #### primary butten
// -> shadow / Schatten
// -> wfst / WFS-T Tool

**** sonstiges Backbone

// -> searchBar
-> weitere?  TODO

**** Anmerkungen zu backbone snippets:
- checkbox -> nicht tastaturbedienbar -> TODO-Tastatur
- slider -> nicht tastaturbedienbar -> TODO-Tastatur
- datepicker -> nicht tastaturbedienbar -> TODO-Tastatur
- multicheckbox -> nicht tastaturbedienbar -> TODO-Tastatur
- checkbox -> nicht tastaturbedienbar -> TODO-Tastatur
- graphicalSelect -> nicht tastaturbedienbar -> TODO-Tastatur
- value -> tastaturbedienbar ok!
- exportButton -> ?

************************* Vue

// -> Tool -> minimized 

// -> AddWMS.vue / WMS hinzufügen
// -> bufferAnalysis / Buffer Analyse
// -> compareFeatures / Vergleichsliste
// -> Contact.vue / Kontakt      #### primary butten
// -> coordToolkit / Koordinaten
// -> Draw.vue /  Zeichnen / Schreiben
// -> FileImport.vue / Datei Import
// -> gfi 
       //-> themes/default
         // -> gfi-iframe (mimeType = text/html, Thema: hamburgEventlotse)
         // -> gfi-theme-images (mimeType = other, z.B. Thema: Krankenhäuser)
       // -> themes/sensor  (Status E-Ladepunkte der E-Ladestationen)
     // -> Attached
     // -> Detached
     // -> Table
     // -> Mobile
// -> Measure.vue / Strecke/Fläche messen
// -> saveSelection / Auswahl speichern
// -> ScaleSwitcher.vue / Maßstab umschalten
// -> SearchByCoord.vue / Koordinatensuche
// -> SelectFeature.vue / Features auswählen
// -> styleVT.vue / Style VT (portal wien)
// -> SupplyCoord.vue / Koordinaten abfragen

**** Vue, diverse

// -> Legend.vue
// -> LayerInformation.vue

***** src/modules/controls (rote controls über der map)

// -> attributions  (Attributierung (info))
// -> backForward
// -> freeze
// -> fullscreen
// -> mousePosition (no control at all?)
// -> orientation (POI) / In der Nähe
// -> overviewmap
// -> totalView
// -> zoom


***************************** AddOns

**** gfiThemes
  
  // -> activeCityMaps
  // -> bildungsatlas
  // -> continuousCountingBike -> portalconfig: Verkehrsportal. Layer: Planung, Verkehrsdaten (infrarot...), Rad
  // -> dataTable -> verwendet keine Farben -> also auch keine Kontrastprobleme
  // -> dipas
  // -> flaecheninfo
  // -> reisezeiten -> fallen weg
  // -> schulinfo
  // -> solaratlas
  // -> trafficCount
  // -> trinkwasser
  // -> verkehrsstaerken

**** Vue-addons

// -> tacticalMark
// -> commuterFlows
// -> schoolrouteplanning
// -> PopulationRequest.vue / Einwohneranzahl abfragen  #### toggle-button tastaturbedienbar gestaltet
   //  -> kein label für Dropdwon.
-> geoAnalyze (portalconfig=AtlasInnereSicherheit) -> aber wir nutzt man das Tool?
// -> fileImportAddon  -> ist bereits im MP repo vorhanden
// -> sdpDownload (portalConfig:fhh-atlas, test-plotfunktion -> startet aber nicht: Reference-Error: Cesium not defined)

**** Backbone AddOns

// -> boris
// -> bauforum (portalConfig: bauforum: startet nicht: Missing or invalid parameter: MAGISTRALE) -> kann laut Sven weggelassen werden
// -> cosi (testbar über cosi-portalconfig)
      // -> Gebiet auswählen
      // -> Dashboard
      -> Simulation?
      // -> Erreichbarkeitsanalyse
      // -> Vergleichbare Gebiete ermitteln
      // -> Versorgungsanalyse
// -> oktagonKartenportal (portalconfig: oktagonKartenportal: Click in Karte)
// -> qr (portalconfig: dipas: Error: Cannot find module './qr/locales/es/additional.json')
-> showParcelGFI (keine portalconfig gefunden) -> was macht tool?




